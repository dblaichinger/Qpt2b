<!DOCTYPE html>
<html>
<head>
  <title>TRASHCANNY | Mülleimer-Patenschaft</title>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
  <link rel="shortcut icon" href="favicon.ico" type="image/x-icon">
  
	  <%= javascript_include_tag :defaults %>
	  <%= javascript_include_tag  'jquery.min', 'jquery.ad-gallery', 'jquery.scrollTo-min', 'jquery.cookie', 'jquery-ui', 'jquery.reversegeocode.min', 'gears_init', 'jquery.class.min', 'editor','jquery.cycle.all.min' %>

  <%= csrf_meta_tag %>
  <%= yield :head %>
  <%= stylesheet_link_tag :all %>
</head>
<body>

<% flash.each do |key, value| %>			
				<div class="flash <%= key %>">
					<%= value %>
				</div>
<% end %>


<div id="wrapper">
	<nav>
	    <div id="logo">
	            <%= link_to image_tag("/images/media/logo.png", :border=>0), pages_home_path %>
	    </div>
	        <!-- , :controller => 'pages', :action => 'home' -->
	    <ul id="nav_haupt">
	        <% if controller?("pages") && action?("home")  %>
	        <li class="active">
	        <%else%><li><% end %>
	        <%=link_to 'Pate werden', pages_home_path %></li>
	    
	        <% if controller?("demands") && action?("index")  %>
	        <li class="active">
	        <%else%><li><% end %>
	        <%= link_to 'Mitbestimmen', demands_path %></li>

	       	<% if controller?("pages") && action?("gallery")  %>
	        <li class="active">
	        <%else%><li><% end %>
	        <%=link_to 'Bildergalerie', pages_gallery_path %></li>

	    </ul>

	</nav>
	
	<div id="kontakt">
		<%=link_to '', contact_path %>
	</div>

	<div id="border_top"></div>

<%= yield %>
<%= yield :scripts %>


</div>

</body>
</html>
